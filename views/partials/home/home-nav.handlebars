    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
      <div class="container-fluid">
        <a class="navbar-brand" href="/"><img src="/images/sparefinsLogo_white.svg" alt="sparefinsLogo"></a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarResponsive" aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse justify-content-end" id="navbarResponsive">
          <ul class="navbar-nav ml-auto">
            <li class="nav-item">
              <a class="nav-link" href="/">Home
                <span class="sr-only">(current)</span>
              </a>
            </li>
            {{#if user.admin}}
            <li class="nav-item">
              <a class="nav-link" href="/admin">Admin</a>
            </li>
            {{/if}}
            {{#unless user}}
            <li class="nav-item">
              <a class="nav-link" href="#modalLoginForm" data-bs-toggle="modal" data-bs-target="#modalLoginForm">Login</a>
              {{> home/login}}
            </li>
            <li class="nav-item">
              <a class="nav-link" href="#modalRegisterForm" data-bs-toggle="modal" data-bs-target="#modalRegisterForm">Register</a>
              {{> home/register}}
            </li>
            {{/unless}}
            </ul>
            {{#if user}}
           <div class="btn-group">
              <button type="button" class="btn btn-dark dropdown-toggle dropdown-toggle-split" data-bs-toggle="dropdown" aria-expanded="false">
                <i class="fa fa-user"></i>
                <span class="visually-hidden">{{user.firstName}}</span>
                {{#if unreadCount}}<span class="badge badge-primary"> {{unreadCount}}</span>{{/if}}
              </button>
              <ul class="dropdown-menu">
    <li><a class="dropdown-item" href="/your-profile/{{user.id}}">Profile</a></li>
    <li><a class="dropdown-item" href="/messages/{{user.id}}">
        Messages {{#if unreadCount}}<span class="badge badge-primary"> {{unreadCount}}</span>{{/if}}
    </a></li>
    <li><hr class="dropdown-divider"></li>
    <li><a class="dropdown-item" href="/logout"><i class="fa fa-sign-out"></i> Log Out</a></li>
</ul>

            {{/if}}
        <a class="btn btn-primary" href="/listings/add-listing" role="button">
          <i class="fa fa-plus"></i>
          Add Listing
        </a>
      </div>
      </div>
    </nav>